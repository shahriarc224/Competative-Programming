class Solution {
  public:
    int largest(vector<int> &arr) {
        
  int l=arr[0];
    for(int i= 0 ; i<arr.size() ; i++){
        
        if(arr[i]>l){
            l=arr[i];
        }
    }
    return l;
        
    }
};
