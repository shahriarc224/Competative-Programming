#include <bits/stdc++.h>
using namespace std;

int main() {
    
    int n,x;
    cin>>n>>x; 
    
      vector<int>v(n);
      
      for (int i=0;i<n; i++) cin>>v[i];
      
     bool a =false;
      
      for (int i=0;i<n; i++) {
          if(x==v[i]) {
              a=true;
              break;
          }
          
      }
	
         if (a) cout<<"YES";
          else cout<<"NO";
	
}
